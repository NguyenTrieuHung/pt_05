'''
Create a python program with 3 threads:
- Main stream for main show
- Stream 1 to show the current time after one second in a loop. This stream will end in 2 minutes,
- Stream 2 inserts 1990 employees into VTI database,
- The show will end once thread 162 completes their task
'''

# employee('id', 'name', 'age')
for i in len(0,1990):
    print(f'name{i}')
